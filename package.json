{
  "name": "stlc-automated-framework",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "scripts": {
    "test": "node scripts/validate-framework.mjs",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "lint": "node --check scripts/validate-framework.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
